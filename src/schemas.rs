pub const ADAM_READ_COLUMNS: &str = "message org.bdgenomics.formats.avro.AlignmentRecord {
        OPTIONAL BINARY        referenceName (UTF8);
        OPTIONAL INT64         start;
        OPTIONAL INT64         end;
}";
